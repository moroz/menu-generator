% vim: syntax=tex
\documentclass{article}

\newcommand{\menuitem}[3]{#1\hfill#2\par}

\begin{document}
{{#each items}}
{{trim name}} {{ #if price }}\hfill {{price}}{{ /if }} \\

{{/each}}
\end{document}

% vim: syntax=tex
\documentclass{article}
\usepackage{fontspec,graphicx,tikz}
\setmainfont[Mapping=tex-text]{IBM Plex Sans}
\usepackage[left=2.5cm,right=2.5cm,top=3cm]{geometry}
\usepackage{nowidow}
\setlength\parindent{0pt}
\setlength\parskip{9pt}
\usepackage{everypage}

\AddEverypageHook{%
  \begin{tikzpicture}[remember picture,overlay]
    \node[anchor=north,yshift=.3cm] at (current page.north)
    {\includegraphics[width=5cm]{./templates/logo.pdf}};
  \end{tikzpicture}}

  \begin{document}
{{#each groups}}
\begin{minipage}{\textwidth}
\section*{ {{~name~}} }

{{#each items}}
{\bfseries {{trim name_pl}}{{#if name_en}} · {{ name_en }}{{ /if }} }{{ #if price }} \hfill {{#if price_for_2}}1 os. {{/if}}{{format_price price}}{{#if price_for_2}}, 2 os. {{format_price price_for_2}}{{/if}}{{ /if }} \\
{{ #if description_pl }}{{ description_pl }}{{ /if }}\\
{{ #if description_en }}{{ description_en }}\\ {{ /if }}

{{/each}}
\end{minipage}
\vfill

{{/each}}
\end{document}

% vim: syntax=tex
\documentclass{article}
\usepackage{fontspec}
\setmainfont[Mapping=tex-text]{IBM Plex Sans}
\usepackage[margin=2cm]{geometry}
\setlength\parindent{0pt}
\setlength\parskip{9pt}

\begin{document}
{{#each items}}
{\bfseries {{trim name_pl}}{{#if name_en}} $\cdot$ {{ name_en }}{{ /if }} }{{ #if price }} \hfill {{format_price price}}{{ /if }} \\
{{ #if description_pl }}{{ description_pl }}\\{{ /if }}

{{/each}}
\end{document}
